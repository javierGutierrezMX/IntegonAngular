import { SortAscDescPipe } from './sort-asc-desc.pipe';

describe('SortAscDescPipe', () => {
  it('create an instance', () => {
    const pipe = new SortAscDescPipe();
    expect(pipe).toBeTruthy();
  });
});
